/*
- When we are using database for different modules that has differnt data and schema etc then we must save that in different files.
- We must have one file for connection and other files for handling the CRUD operations.

? What is the issue of CORS..?
- When we do get post and put with postman it works properly but when we try to do any database related operations using the UI or frontend it throws an cors error.

-   

*/